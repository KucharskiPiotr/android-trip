package com.agh.trip.backend.data.dto

data class PaymentData(val id: Int,
                       val name: String,
                       val description: String,
                       val amount: Double)